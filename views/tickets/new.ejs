<% layout('layouts/boilerplate') -%>
    <h1>New ticket</h1>
    <form action="/tickets" method="POST">
        <div>
            <label for="title">Title</label>
            <input type="text" id="title" name="ticket[title]">
        </div>
        <div>
            <label for="description">Description</label>
            <textarea name="ticket[description]" id="description" cols="30" rows="10"></textarea>
        </div>
        <div>
            <label for="reproduceBug">Steps to reproduce bug</label>
            <textarea name="ticket[reproduceBug]" id="reproduceBug" cols="30" rows="10"></textarea>
        </div>
        <button>Submit</button>
    </form>
    <a href="/tickets">All tickets</a>
